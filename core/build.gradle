group 'org.mariotaku.logansquare.extension'

apply plugin: 'java'

ext.artifactId = 'logansquare-extension-core'

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.bluelinelabs:logansquare:1.1.0'
}